#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>

int _putchar(char c);
void _print_s(char *str);
int _printf(const char *format, ...);
int putchr(char c);
int _stringoutput(char *str);
int _strlen(char *s);

#endif
