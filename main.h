#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdio.h>
#include <stdarg.h>
#include <string.h>

int _putchar(char c);
void _print_s(char *str);
int _printf(const char *format, ...);
int _stringoutput(char *str);
int _strlen(char *s);

#endif
